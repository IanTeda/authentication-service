# Server configuration
server:
  port: 8081
  ip_address: "0.0.0.0"
  log_level: "info"

# Postgres database config
database:
  host: "postgres"
  port: 5432
  username: "postgres"
  password: "postgres"
  database_name: "authentication"
  require_ssl: false